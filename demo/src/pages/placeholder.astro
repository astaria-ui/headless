---
import { APlaceholder } from "@astaria/placeholder";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Astaria Placeholder Demo (Radix UI Style)</title>
    <link href="/path/to/your/global.css" rel="stylesheet" />
  </head>
  <body
    class="p-8 font-sans bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100"
  >
    <h1 class="text-4xl font-bold mb-8">
      Astaria Placeholder Primitives (Radix UI Style)
    </h1>
    <p class="mb-8 text-gray-600 dark:text-gray-400">
      These placeholders are unstyled by default, relying entirely on external
      CSS. They expose `part="container"` for styling.
    </p>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
      <div
        class="flex flex-col items-start space-y-4 p-4 border rounded-lg shadow-sm bg-gray-50 dark:bg-gray-800"
      >
        <h2 class="text-xl font-semibold mb-2">
          Basic Placeholders (Styled via Global CSS)
        </h2>
        <APlaceholder id="placeholder-1" width={200} height={20} />
        <APlaceholder id="placeholder-2" width="80%" height="20px" />
        <APlaceholder id="placeholder-3" width="150px" aspectRatio={2} />
      </div>

      <div
        class="flex flex-col items-start space-y-4 p-4 border rounded-lg shadow-sm bg-gray-50 dark:bg-gray-800"
      >
        <h2 class="text-xl font-semibold mb-2">
          Pulsating Placeholders (Styled via Global CSS)
        </h2>
        <APlaceholder
          id="placeholder-pulse-1"
          width="100%"
          height="20px"
          pulse
        />
        <APlaceholder
          id="placeholder-pulse-2"
          width="70%"
          height="20px"
          pulse
        />
        <APlaceholder
          id="placeholder-pulse-3"
          width="100px"
          height="100px"
          aspectRatio={1}
          pulse
        />
      </div>

      <div
        class="flex flex-col items-start space-y-4 p-4 border rounded-lg shadow-sm bg-gray-50 dark:bg-gray-800"
      >
        <h2 class="text-xl font-semibold mb-2">
          Circular Placeholders (Styled via Global CSS)
        </h2>
        <APlaceholder id="placeholder-circle-1" width="60px" height="60px" />
        <APlaceholder
          id="placeholder-circle-2"
          width="80px"
          height="80px"
          pulse
        />
      </div>
    </div>

    <style>
      astaria-placeholder {
        display: inline-block;
      }

      astaria-placeholder::part(container) {
        background-color: var(--astaria-placeholder-background-color, #e5e7eb);
        border-radius: 0.375rem;
      }

      #placeholder-1::part(container) {
        background-color: #bfdbfe;
      }

      #placeholder-2::part(container) {
        background-color: #a7f3d0;
      }

      #placeholder-3::part(container) {
        background-color: #fde68a;
        border-radius: 50%;
      }

      #placeholder-pulse-1::part(container),
      #placeholder-pulse-2::part(container) {
        background-color: #d1d5db;
      }

      #placeholder-pulse-3::part(container) {
        background-color: #d1d5db;
        border-radius: 50%;
      }

      #placeholder-circle-1::part(container) {
        background-color: #fecaca;
        border-radius: 50%;
      }
      #placeholder-circle-2::part(container) {
        background-color: #fecaca;
        border-radius: 50%;
      }
    </style>
  </body>
</html>
